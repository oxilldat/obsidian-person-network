{
	"name": "obsidian-person-network",
	"version": "1.0.1",
	"description": "Visualize the people in your vault as an interactive relationship map",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"keywords": [],
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^22.0.0",
		"esbuild": "^0.24.0",
		"obsidian": "latest",
		"tslib": "2.8.1",
		"typescript": "5.7.2",
		"vitest": "^2.1.0"
	},
	"dependencies": {}
}
